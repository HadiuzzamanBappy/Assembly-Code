#include<stdio.h>
int main()
{
    int a,b,sum;
    while(scanf("%d%d",&a,&b)!=EOF)
    {
        if(1<=a<=300 && 1<=b<=300)
        {
            sum=n*m-1;
            printf("%d\n",sum);
        }
    }
    return 0;
}
